Used components: file, http4, jms, validator, xslt, marshal, split, filter, direct, xpath, transform, mock, processor and errorHandler and deadLetterChannel
    using camel DSL.


I need to use the webservice.rar (Tomcat with the service), located on root source, to run this project.
